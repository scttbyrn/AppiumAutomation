pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "Scott"'
                sh 'date'
            }
        }
    }
}
